const inquirer = require("inquirer");
const fs = require("fs");
const shapes = require("./shapes");
const { initalizeApp } = require('../index.js');


// Test that the initalizeApp function prompts the user for input.

// Test that the initalizeApp function generates the correct SVG markup based on user input.

// Test that the initalizeApp function writes the SVG markup to a file